# Worklog — журнал работы

Живой журнал: решения, задачи AI, проверки, найденные проблемы. Append-only, короткие датированные записи с тегом. Из этого журнала в конце собирается `AI_USAGE.md`.

**Теги:** РЕШЕНИЕ · AI-ЗАДАЧА · ПРОВЕРКА · ПРОБЛЕМА
**Инструменты:** Claude Code, ChatGPT Codex.

Записи «ПРОВЕРКА» отражают только то, что реально запускалось и сверялось.

---

## 2026-07-01 · Проектирование (до кода)

**РЕШЕНИЕ** — Backend: FastAPI + SQLAlchemy + Alembic (без pandas).
  Почему: лёгкий async-фреймворк с авто-документацией API (Swagger); SQLAlchemy-модели = целевая схема, Alembic — миграции; для маленькой ETL-задачи pandas избыточен, CSV читаем обычным Python.
  Отклонено: Django (batteries-included, но здесь не нужны его ORM/admin/шаблоны в комплекте).

**РЕШЕНИЕ** — Слои `raw (TEXT, без ограничений) → core (типы, PK/FK/CHECK)` + справочник `sources` + таблица `data_quality_issues`.
  Почему: выгрузка намеренно содержит грязные данные; нельзя ронять импорт кривой датой/суммой и нельзя молча терять строки — каждую проблему логируем.

**РЕШЕНИЕ** — Проверка связей и решения (fix/quarantine/flag) — в Python-transform до вставки; ограничения БД в core — страховка целостности.

**РЕШЕНИЕ** — Зафиксированы бизнес-решения по отчётам (сумма сделки = гибрид; дебиторка = только `paid`; «без активности» N=14; скидка = рубли; WON без даты — флаг). Обоснование — в `ASSUMPTIONS.md`.

**ПРОБЛЕМА** — Предварительный разбор выгрузки: найдено 28 дефектов качества (полный каталог — `DATA_QUALITY.md`). В т.ч. два неочевидных, выявленных сверкой расчётов: переплата по сделке D1005 (оплачено 100000 при сумме 92000) и производственный заказ PO003, созданный на 4 дня раньше своей сделки.

**ПРОВЕРКА** — Эталонные расчёты (выручка/маржа по позициям, дебиторка paid-only, задержки производства) прогнаны вручную до реализации — будут использованы для сверки будущих SQL-отчётов.

## 2026-07-01 · Уточнения по итогам обсуждения с владельцем

**РЕШЕНИЕ** — Дебиторка: платёж типа `correction` уменьшает долг, а не «оплачено». Остаток = `сумма − оплачено + Σ correction` (correction со знаком). D1010: единственная запись `correction −5000` при сумме 174000, обычных оплат нет → остаток 169000. В отчёте — отдельная колонка «корректировка».
  Отклонено: прежняя трактовка «минус уменьшает оплачено» (давала остаток 179000 — долг больше суммы сделки, бессмыслица). Правит `ASSUMPTIONS §2`.

**РЕШЕНИЕ** — Дата среза («сегодня») = **2026-06-15** = максимум по фактическим событиям (`updated_at/payment_date/actual_date/completed_at/changed_at`), без будущих плановых дат (`custom_deadline`, `planned_finish`). Иначе PO001/D1001 (план 12.06, факта нет) ложно попал бы в «задержку производства». Одна дата среза на отчёты 4 и 5.

**РЕШЕНИЕ** — Маржа/продажи по менеджерам — по WON; SHIPPED (D1005 — отгружена и оплачена, но не закрыта) показываем отдельной пометкой «отгружено, не закрыто»: не теряем фактическую продажу и не смешиваем с закрытыми.

**РЕШЕНИЕ** — «Последнее касание» для отчёта «без активности» = только записи `activities` (deadline/completed). `stage_history` и `deal.updated_at` касанием не считаем (иначе почти все сделки «свежие», отчёт при N=14 пуст).

**РЕШЕНИЕ** — Источники: при отсутствии строк `marketing_costs` по каналу ROMI не считаем делением на ноль — показываем «нет затрат» (referral/telegram/phone).

**РЕШЕНИЕ** — Порядок фаз изменён: живой контур (минимальный API + React + деплой за туннелем) поднимаем рано, сразу после core+transform; 7 отчётов делаем вертикальными срезами (SQL→API→страница) с инкрементальным деплоем; финал-доки и опц. Metabase — в конце. Причина: владелец хочет верифицировать данные и отчёты вживую по ходу, а не в самом конце.

**РЕШЕНИЕ** — Данные/инфраструктура: исходные CSV в git не коммитим (лежат локально в `data/`, всё живёт в БД); в README — инструкция «положи 13 CSV в `data/` → `make load`». Postgres на порту 5433. Все таблицы создаём средствами Python (SQLAlchemy + Alembic); `schema.sql` = дамп схемы. Metabase — в самый конец. Git-почта владельца остаётся как есть, историю не переписываем (почты в таблице `users` в git не попадут, т.к. данные не коммитим).

**ПРОВЕРКА** — Пересчитал дебиторку под новое правило корректировок: D1010=169000, D1005=−8000 (переплата), D1001=66000, D1002=84000 (pending 30000), D1003=0 — сходится с эталонами. Решения приняты до кода, по согласованию с владельцем.

## 2026-07-01 · Шаг 0 — каркас

**AI-ЗАДАЧА** — Сгенерировать каркас: docker-compose (postgres:16-alpine), Makefile, backend-скелет (FastAPI + SQLAlchemy + Alembic; config/session/main), `.env(.example)`.

**ПРОБЛЕМА → РЕШЕНИЕ** — Порты 5433 и 5434 заняты другими локальными проектами (проверено `ss -ltn`). Postgres перевели на **5435**; поправлены README/AGENTS/.env.

**ПРОВЕРКА** (реально запускалось):
  - `docker compose up -d db` → контейнер `healthy`; `psql` → PostgreSQL 16.14, схема пустая.
  - `make venv` → зависимости установлены (fastapi/sqlalchemy/alembic/psycopg3/uvicorn).
  - `app.db.session.engine` → `select 1` = 1 (приложение видит БД).
  - `alembic current` → env.py грузится, коннект к БД, exit 0 (ревизий пока нет).
  - `import app.main` + `health()` → `{'status':'ok','db_reachable':True}`.

## 2026-07-01 · Шаг 1 — raw-слой

**AI-ЗАДАЧА** — raw-модели (13 таблиц, все колонки TEXT, суррогатный PK, без ограничений) + загрузчик CSV→raw (идемпотентный: TRUNCATE перед вставкой; пустая ячейка → NULL) + Alembic-миграция.

**ПРОВЕРКА** (реально запускалось):
  - `alembic upgrade head` → создано 13 `raw_*` таблиц (+ alembic_version).
  - `make load` → загружено 80 строк.
  - Независимая сверка БД vs CSV (csv-парсер по каждой таблице): все 13 совпали построчно, итого **80 = 80**.
  - Спот-чек «как есть»: `D1010.created_at='14.06.2026 09:20'` и пустая `currency`→NULL сохранены; дубль D1008 (68000/69000) на месте; D1007 `-12000` сохранён; C205 пустые поля→NULL; PAY006 `2026/06/15` / `-5000` сохранены; сирота D9999 присутствует в raw_deal_products.

## 2026-07-01 · Шаг 2 — core-слой (целевая схема)

**AI-ЗАДАЧА** — core-модели SQLAlchemy (настоящие типы, натуральные PK после дедупа, FK между сущностями, CHECK на доменах статусов) + наши добавления `sources` (справочник каналов) и `data_quality_issues` (лог проблем, поля по BRIEF §4: entity/entity_id/issue_type/action/details + detected_at) + `has_quality_issue` на бизнес-строках. Alembic-миграция с сидом справочника `sources`. Без transform — только схема (данные в core придут на шаге 3).

**ПРОВЕРКА (сам, по данным — до кода)** — распределение значений в raw, чтобы CHECK/сид были по факту, а не по памяти: каналы `deals.source` (AVITO/Avito/avito/Авито/Website/website/phone/referral/telegram/yandex_direct) → канон 6 шт. в нижнем регистре `avito, website, yandex_direct, phone, referral, telegram`; стадии — 9 канонических, `WAIT_CLIENT` вне справочника (D1011) → stage_id NULL+флаг; домены: payments.status∈{paid,pending}, type∈{prepayment,full,correction,unknown}; production∈{planned,in_progress,done}; shipments∈{planned,shipped}; activities.type∈{call,email,task}.

**РЕШЕНИЕ** — В `deals` храним `expected_amount` (оценка из карточки, честный факт), а гибрид-сумму `COALESCE(Σпозиций, expected_amount)` считаем в отчётах (уточняет псевдокод BRIEF §6, где поле называлось `amount`). Причина: не запекать производную величину в базовую таблицу.

**РЕШЕНИЕ** — `data_quality_issues`: имена полей взяты из BRIEF §4 (`entity/entity_id/details`), а не из свободной чатовой формулировки (`table_name/row_key/raw_value`) — это та же согласованная схема; исходное «грязное» значение кладём в `details`. Добавлено неломающее `detected_at default now()`.

**РЕШЕНИЕ** — Сид `sources` — внутри Alembic-миграции (`op.bulk_insert`, без импорта app-моделей), чтобы `alembic upgrade head` сразу давал заполненный справочник (страховка «схема поднята = справочник готов»). `pipeline_stages` не сидируем — это реальные данные Bitrix, грузятся из CSV на шаге transform.

**ПРОВЕРКА (реально запускалось):**
  - `alembic upgrade head` → создано 15 core-таблиц; ревизия `69c662503046 (head)`.
  - Обратимость: `alembic downgrade -1` → `upgrade head` без ошибок.
  - В БД: 15 core-таблиц, **19 FK**, 6 именованных CHECK (dqi_action, payment_type, payment_status, activity_type, production_status, shipment_status). `sources` = 6 строк, коды в нижнем регистре.
  - Ограничения работают: FK отклонил `deal_products` на несуществующую сделку; CHECK отклонил `payments.status='weird'`; сделка с отрицательной суммой (бизнес-сигнал, `has_quality_issue=true`) — прошла. Тестовые строки удалены/откатились, core-таблицы пусты (кроме сида).

## 2026-07-01 · Шаг 3 — transform (raw → core)

**AI-ЗАДАЧА** — `etl/clean.py` (чистые функции: norm_source/parse_dt/parse_date/norm_phone/parse_amount/parse_int/parse_bool/norm_inn) + `etl/transform.py` (обобщённый проход raw→core: дедуп, проверка связей, три реакции fix/quarantine/flag с логом в data_quality_issues) + обвязка (run.py, `make transform`, `make test`) + юнит-тесты чистилок.

**РЕШЕНИЕ** — Логика ОБОБЩЁННАЯ, без привязки к конкретным строкам выгрузки: дубли — группировкой по ключу (deals по deal_id/max updated_at; stage_history по идентичности), сироты — проверкой существования ссылки в множествах валидных ключей, нелегальные переходы — по FSM-карте, домены — по справочникам. Конкретные ID (D9999/D1001/PR006…) фигурируют только в сверке, а не в коде.

**РЕШЕНИЕ** — has_quality_issue на строке ставим для flagged/quarantined и «заметных» fix (кривая дата, пустая валюта, формат телефона), но НЕ для косметической нормализации регистра канала (иначе флагуются чистые сделки).

**ПРОБЛЕМА → РЕШЕНИЕ (FSM).** Строгая карта «к WON только через SHIPPED» ложно пометила EVT004 (D1003 PROPOSAL→WON) как illegal_transition — а D1003 легитимно выиграна. Исправлено обобщённо: терминальные WON/LOST достижимы из любой активной стадии (сделку можно закрыть в любой момент); нарушение — форвардные прыжки, пропускающие стадии (D1001 QUALIFICATION→PRODUCTION). После правки illegal_transition остаётся только D1001 — совпадает с каталогом.

**ПРОБЛЕМА → РЕШЕНИЕ (FK-порядок).** У core-моделей нет relationship(), поэтому один flush на commit не гарантировал порядок вставки между таблицами (дети вставлялись раньше deals → ForeignKeyViolation). Исправлено: грузим в FK-безопасном порядке и `session.flush()` после каждого уровня справочников и после deals.

**ПРОБЛЕМА → РЕШЕНИЕ (даты).** Чисто-датные поля («2026-06-08») лежали в timestamptz как полночь +05 и при `::date` в UTC-сессии откатывались на день назад (ломало дату среза = max по событиям). Перевёл `payments.payment_date`, `deals.custom_deadline`, `production_orders.planned_finish_at/actual_finish_at` в тип `Date` (миграция 044fd52c748a); остальные даты — настоящий ISO+TZ, оставлены timestamptz.

**ПРОВЕРКА (реально запускалось):**
  - `pytest` — 21 тест чистилок зелёные (регистр/алфавит каналов, ISO vs кривые даты, телефон 8→+7, Decimal, ведущие нули ИНН).
  - `make load` (raw→core) без ошибок. Счётчики core: users 5 / companies 6 / contacts 5 / products 6 / stages 9 / **deals 11** (дедуп D1008) / **deal_products 8** (карантин сирот D9999 и PR999) / **payments 5** (карантин PAY005) / **stage_history 4** (дедуп EVT004, карантин EVT005) / **activities 3** / **production_orders 3** / shipments 3 / marketing_costs 4.
  - `data_quality_issues` = **29** (fixed 9 / flagged 12 / quarantined 8) — покрывают каталог из 28 дефектов; структурные/report-time пункты (ИНН, correction, скидка-непревышение, переплата, unknown-тип на сироте) объяснены отдельно.
  - Сверка с Приложением B: **маржа по позициям** D1001=39100 / D1002=42400 / D1003=85600 / D1004=13800 / D1005=48500 — точно; **дебиторка-ингредиенты** остатки D1001=66000 / D1003=0 / D1005=−8000 (переплата) / D1010=169000, D1002 pending 30000 — сходится; **задержка** PO003=6 дн, PO002=2 дн; **дата среза** = 2026-06-15.
  - Спот-чек значений: D1008→69000/website, D1010 created_at=09:20+05 и currency RUB, D1011 stage/manager NULL, P303 телефон +79170003003, P304 company NULL, C200 ИНН 0278123456, PR006 canonical=PR001, все сделки с разрешённым source_id.

**ПРОВЕРКА (независимое ревью кода)** — Отдельным проходом проверены `transform.py`/`clean.py` на два риска: хардкод конкретных строк (не найдено — вся логика обобщённая, ID фигурируют только в комментариях-предупреждениях) и корректность/обобщаемость. Найденное исправлено:
  - **P1 (крит.):** значения вне CHECK-домена (`payment_type/status`, `activity_type`, production/shipment `status`) пробрасывались сырыми и на ДРУГИХ данных уронили бы весь проход по CHECK. Добавлены домены-константы + `_in_domain()`: значение вне домена → NULL (проходит CHECK) + лог `invalid_*`. Проверено впрыском `refund/cancelled/meeting/queued/returned` в raw — проход не падает, 5 `invalid_*` залогированы, поля занулены.
  - **P2:** дедуп сделок переписан на группировку — победитель детерминирован (max updated_at, тай-брейк по raw.id), в лог идут только вытеснённые (раньше победитель мог попасть в quarantined; при непарсящемся updated_at выбор был недетерминирован).
  - **P3:** товары грузятся `order_by(product_id)` — самоссылочный FK `canonical_id` не зависит от порядка строк; `normalized_source` больше не логируется как «fixed» для неизвестного канала (раньше давал двойной лог fixed+unknown при NULL source_id).
  - **P4:** сироты `changed_by_id`/`responsible_user_id` теперь логируются (консистентно с manager); `parse_bool` при мусоре → None (не False).
  - **P4c:** `norm_phone` получил третье состояние `"unrecognized"` (номер с добавочным/12+ цифр); transform пишет `unnormalized_phone` (flagged), значение хранит как есть. Проверено впрыском добавочного номера — флаг ставится, данные не теряются.
  - После правок: pytest 23 зелёных; core-счётчики и `data_quality_issues`=29 (9/8/12) не изменились — правки закрыли краевые случаи на других данных, не тронув проверенный результат этой выгрузки. Все замечания ревью закрыты.

## 2026-07-01 · Синхронизация плана (перед Шагом 4)

**РЕШЕНИЕ** — `BRIEF §10` приведён в соответствие с пересмотренным порядком фаз (`AGENTS.md` «План внедрения»): живой контур + деплой рано (Шаг 4), 7 отчётов — вертикальными срезами SQL→API→страница (Шаг 5). Прежний линейный порядок BRIEF (отчёты→API→фронт→деплой в конце) отменён — убрана рассогласованность между документами. Источник правды по фазам — `AGENTS.md` + синхронизированный `BRIEF §10`. Владелец подтвердил: деплой в Шаге 4 делаем «локально → затем туннель в этом же шаге».

## 2026-07-01 · Шаг 4 — живой контур (API + React + docker)

**AI-ЗАДАЧА** — Сквозной read-only контур поверх core: FastAPI-роуты (`/api/core/{table}` — витрина по белому списку + `/api/data-quality/summary|issues` = отчёт №7), React (Vite)+Recharts дашборд (2 экрана: «Качество данных» — график по типам×реакция + таблица 29 проблем; «Просмотр core» — сайдбар 15 таблиц + грид с подсветкой флаг-строк), docker-compose api+web(nginx).

**РЕШЕНИЕ (порты)** — 8000 занят другим локальным проектом → локальный uvicorn и хостовый api на **8010**; web(nginx) на **8090**. Внутри compose api ходит в БД как `db:5432` (хостовый 5435 — только маппинг). Фронт всегда зовёт относительный `/api/*`: в dev проксирует Vite на :8010, в проде nginx на контейнер api — один origin, без CORS-возни.

**РЕШЕНИЕ (сериализация/доступ)** — Decimal→строка (точность денег/ИНН), даты→ISO. Витрина core — по белому списку моделей (нельзя запросить произвольную таблицу; неизвестная → 404). `data_quality_issues` отдаём и агрегатом (карточки/график), и списком (детальная таблица).

**ПРОВЕРКА (реально запускалось):**
  - import-smoke `app.main`: зарегистрированы 5 роутов (`/api/core/tables`, `/api/core/{table}`, `/api/data-quality/summary|issues`, `/health`).
  - uvicorn:8010 → сверка API vs БД: все 15 таблиц счётчики совпали (deals 11, dq 29); `/api/data-quality/summary` total=29 (fixed 9 / quarantined 8 / flagged 12); спот-чек deals: D1008 дедуп→69000, Decimal строкой, даты ISO, D1010 has_issue=true; unknown-таблица→404; issues=29 строк.
  - Frontend: `npm run build` зелёный; Vite dev + прокси `/api` работает; Playwright-снимки обоих экранов — **0 JS-ошибок**, данные корректны (29 проблем; 11 сделок; флаг-строки подсвечены).
  - Docker-контур: `docker compose build` (api+web) ок; `up -d` → db healthy, api/web Up. Через **nginx:8090** сквозная цепочка: SPA отдаётся, `/api/data-quality/summary`=29, `/api/core/tables`=15/deals=11, SPA-фоллбэк 200, api-лог показывает проксирование от web-контейнера. Скриншот прод-сборки через nginx — идентичен dev.

**ОТКРЫТО (ждёт владельца)** — Деплой за туннелем: dm312sv-туннель (pm2, изолированный аккаунт) уже обслуживает `business-up:8099` и `sitepro:8100`. План — добавить ingress `termosphere.dm312sv.online → 127.0.0.1:8090` + DNS-route + рестарт `dm312sv-cloudflared` (кратко затронет соседние проекты). gtmoffice-туннель НЕ трогаем. Нужно подтверждение владельца и решение по аутентификации (публичный доступ к тестовым CRM-данным).

<!-- Далее — записи по шагам реализации (см. AGENTS.md § План внедрения). -->
