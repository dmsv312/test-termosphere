# Локальные рабочие заметки — не публиковать
BRIEF.md
BRIEF.notes.md

# Исходные данные — в git не коммитим (лежат локально в data/, всё живёт в БД)
Таблица выгрузки Битрикс.pdf
data/*.csv
data/*.xlsx

# Секреты деплоя — только на хосте
deploy/htpasswd

# Окружение и сборка
.env
.env.*
!.env.example
__pycache__/
*.pyc
.venv/
venv/
node_modules/
dist/
